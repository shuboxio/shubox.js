<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <script src="shubox_config.js"></script>
  <title>Shubox: <%= currentPage.title %></title>
</head>

<body>
  <main>
    <div class="sidebar">
      <ul class="nav">
        <% pages.forEach(page => { %>
          <li>
            <a href="<%= page.path %>" class="<%= page.path == currentPage.path ? " active " : " " %>">
              <%= page.title %>
            </a>
          </li>
          <% }) %>
      </ul>
    </div>

    <div class="container">
